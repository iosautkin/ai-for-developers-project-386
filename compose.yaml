services:
  app:
    build: .
    environment:
      DATABASE_PATH: /data/calendar.sqlite
      PORT: ${PORT:-3000}
    ports:
      - '${PORT:-3000}:${PORT:-3000}'
    volumes:
      - calendar-data:/data

volumes:
  calendar-data:
