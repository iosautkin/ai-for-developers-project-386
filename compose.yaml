services:
  app:
    build: .
    environment:
      DATABASE_PATH: /data/calendar.sqlite
      PORT: 3000
    ports:
      - '3000:3000'
    volumes:
      - calendar-data:/data

volumes:
  calendar-data:
